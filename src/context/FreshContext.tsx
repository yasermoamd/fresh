import {createContext} from "preact";


export const FreshContext = createContext({
    showSaveBtn: false,
    setShowSaveBtn: () => {}
})


export const FreshProvider = (children: ) => {
    return ()
}